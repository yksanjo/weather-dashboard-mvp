{
  "name": "weather-dashboard-mvp",
  "version": "1.0.0",
  "description": "Beautiful Weather Dashboard with Visualizations MVP",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["weather", "dashboard", "visualization", "forecast"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

